#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
/**
 * 1. 고민 -> 1~2시간
 * 2. 문제 풀기
 *    1. 최소/최대 범위 확인
 *    2. 일단 무식하게 푼다. 중요한 건 시간 복잡도를 파악하는 것
 *        - 단순 구현이라면, 반례 생각
 *    3. 시간 복잡도가 안되거나, 단순 구현 문제가 아니라면, 다른 알고리즘 생각
 * 3. 답지 확인
 *    - 틀렸으면, 일주일 내내 처음부터 풀기 [처음 보자마자 30분 내로 풀 수 있을 정도로]
 */

// 성의 첫 글자가 같은 선수 5명 선발. [만약, 5명보다 적다면, 기권]
// 다섯명 선발 못할 경우 "PREDAJA" 리턴, 가능할 경우 가능한 성의 첫 글자 사전순으로 공백없이 모두 출력
/**
 * 선수의 수 -> 1 <= N <= 150
 * 문자열은 모둔 알파펫 소문자로 이루어져 있음 + 최대 30글자
 */
string PREDAJA = "PREDAJA";
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  return 0;
}